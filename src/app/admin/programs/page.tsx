import { getDb } from "@/lib/db";import { PageHeading } from "@/components/page-heading";import { EntityManager } from "@/components/entity-manager";export default async function Page(){const rows=await getDb().program.findMany({orderBy:{name:"asc"}});return <><PageHeading eyebrow="Academic structure" title="Programs" description="Programs organize subjects, students and subject offerings."/><EntityManager kind="program" rows={rows}/></>}
