import{getDb}from"@/lib/db";import{PageHeading}from"@/components/page-heading";import{Card,CardContent}from"@/components/ui/card";import{Badge}from"@/components/ui/badge";export default async function Page(){const rows=await getDb().user.findMany({include:{program:true,semester:true,section:true},orderBy:{createdAt:"desc"}});return <><PageHeading eyebrow="Access" title="Users" description="Student and administrator accounts with academic profile context."/><Card className="border-white/7 bg-white/[.025]"><CardContent className="divide-y divide-white/6 p-0">{rows.map(x=><div key={x.id} className="flex justify-between p-4"><div><p className="font-medium">{x.name}</p><p className="mt-1 text-xs text-slate-500">{x.email} · {x.program?.name??"Administration"}</p></div><Badge variant="outline">{x.role}</Badge></div>)}</CardContent></Card></>}
