import { getDb } from "@/lib/db";import { PageHeading } from "@/components/page-heading";import { EntityManager } from "@/components/entity-manager";export default async function Page(){const rows=await getDb().section.findMany({orderBy:{name:"asc"}});return <><PageHeading eyebrow="Academic structure" title="Sections" description="Each offering belongs to one exact class section."/><EntityManager kind="section" rows={rows}/></>}
