import { getDb } from "@/lib/db";import { PageHeading } from "@/components/page-heading";import { EntityManager } from "@/components/entity-manager";export default async function Page(){const rows=await getDb().semester.findMany({orderBy:{order:"asc"}});return <><PageHeading eyebrow="Academic structure" title="Semesters" description="Semester order drives student filtering and GPA history."/><EntityManager kind="semester" rows={rows}/></>}
