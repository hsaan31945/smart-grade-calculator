import { getDb } from "@/lib/db";import { PageHeading } from "@/components/page-heading";import { EntityManager } from "@/components/entity-manager";export default async function Page(){const rows=await getDb().teacher.findMany({orderBy:{name:"asc"}});return <><PageHeading eyebrow="Faculty" title="Teachers" description="Teacher identity is part of every subject offering."/><EntityManager kind="teacher" rows={rows}/></>}
