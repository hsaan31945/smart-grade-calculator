import{getDb}from"@/lib/db";import{PageHeading}from"@/components/page-heading";import{StatisticsManager}from"@/components/statistics-manager";export default async function Page(){const offerings=await getDb().subjectOffering.findMany({include:{subject:true,section:true,statistics:{where:{componentId:null}}}});return <><PageHeading eyebrow="Relative grading inputs" title="Class statistics" description="Enter the class average and standard deviation once per exact subject offering. Students never enter these values."/><StatisticsManager offerings={offerings}/></>}
