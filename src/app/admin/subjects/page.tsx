import{getDb}from"@/lib/db";import{PageHeading}from"@/components/page-heading";import{SubjectManager}from"@/components/subject-manager";export default async function Page(){const db=getDb();const[rows,programs,semesters]=await Promise.all([db.subject.findMany({include:{program:true,semester:true}}),db.program.findMany(),db.semester.findMany({orderBy:{order:"asc"}})]);return <><PageHeading eyebrow="Course catalog" title="Subjects" description="Base course details. Offering-specific policies are configured separately."/><SubjectManager rows={rows} programs={programs} semesters={semesters}/></>}
