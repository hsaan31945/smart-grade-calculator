import{notFound}from"next/navigation";import{getDb}from"@/lib/db";import{PageHeading}from"@/components/page-heading";import{OfferingEditor}from"@/components/offering-editor";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const o=await getDb().subjectOffering.findUnique({where:{id},include:{subject:true,section:true,teacher:true,components:{orderBy:{sortOrder:"asc"}}}});if(!o)notFound();return <><PageHeading eyebrow="Offering policy" title={o.subject.name} description={`${o.section.name} · ${o.teacher.name} · ${o.academicTerm}. Components below belong only to this offering.`}/><OfferingEditor offering={o}/></>}
