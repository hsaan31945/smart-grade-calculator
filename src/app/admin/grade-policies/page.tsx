import{getDb}from"@/lib/db";import{PageHeading}from"@/components/page-heading";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";export default async function Page(){const rows=await getDb().relativeGradePolicy.findMany({include:{boundaries:{orderBy:{sortOrder:"asc"}}}});return <><PageHeading eyebrow="Z-score boundaries" title="Relative grade policies" description="Policies are reusable boundary sets; each offering explicitly chooses one."/><div className="grid gap-5 lg:grid-cols-2">{rows.map(p=><Card key={p.id} className="border-white/7 bg-white/[.025]"><CardHeader><CardTitle>{p.name}</CardTitle></CardHeader><CardContent className="space-y-2">{p.boundaries.map(b=><div key={b.id} className="grid grid-cols-3 rounded-lg border border-white/6 p-3 text-sm"><span className="font-medium">{b.grade}</span><span className="text-slate-400">z ≥ {b.minZScore}</span><span className="text-right font-mono text-cyan-300">{b.gradePoint.toFixed(1)}</span></div>)}</CardContent></Card>)}</div></>}
