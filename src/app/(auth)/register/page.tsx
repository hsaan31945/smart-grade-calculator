import { AuthForm } from "@/components/auth-form"; import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"; import { getDb } from "@/lib/db";
export default async function RegisterPage(){const db=getDb(); const [programs,semesters,sections]=await Promise.all([db.program.findMany(),db.semester.findMany({orderBy:{order:"asc"}}),db.section.findMany()]);return <Card className="border-white/7 bg-white/[.025]"><CardHeader><CardTitle className="text-2xl">Create student account</CardTitle><CardDescription>Your academic profile filters the subject offerings you see.</CardDescription></CardHeader><CardContent><AuthForm mode="register" programs={programs} semesters={semesters} sections={sections}/></CardContent></Card>}
